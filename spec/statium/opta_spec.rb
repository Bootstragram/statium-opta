require "spec_helper"

describe Statium::Opta do
  it "has a version number" do
    expect(Statium::Opta::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
